apiVersion: apps/v1
kind: Deployment

metadata:
  name: web-deployment

spec:
  replicas: 4
  template:
    metadata:
      labels:
        component: dockerkube-app
    spec:
      containers:
        - name: dockerkube-app-container
          image: manojkumar9953873435/dockerkube:latest
          ports:
          - containerPort: 80
          env:
            - name: POSTGRES_HOST
              value: postgres-service
            - name: POSTGRES_USER
              value: docker_kube
            - name: POSTGRES_PORT
              value: '5432'
            - name: POSTGRES_DB
              value: docker_kube_db
            - name: PGPASSWORD
              valueFrom:
                secretKeyRef:
                  name: pgpassword
                  key: PGPASSWORD

  selector:
    matchLabels:
      component: dockerkube-app